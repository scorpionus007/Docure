requests
# Use python-magic-bin on Windows, python-magic elsewhere
python-magic-bin; platform_system == "Windows"
python-magic; platform_system != "Windows"
yara-python
pefile
lief>=0.13.0
python-dotenv
google-genai
# Optional: Unipacker for multi-packer unpacking support (UPX, ASPack, FSG, MEW, MPRESS, PEtite, YZPack)
# unipacker

